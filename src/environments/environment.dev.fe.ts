import 'zone.js/plugins/zone-error';
import { Environment } from './environment-model';

export const environment: Environment = {
  serverUrl: "/PromoPlanning300/",
  production: false,
  local_id: 'en-US',
};