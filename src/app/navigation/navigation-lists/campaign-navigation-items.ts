import { NavigationLinks } from "../navigation-links";
import { PpNavigation } from "../navigation.model";

export const campaignNavigationItems: PpNavigation[] = [
    PpNavigation.create(1, 'Campaigns', NavigationLinks.campaigns, "list", false, 0, "Campaigns"),
]